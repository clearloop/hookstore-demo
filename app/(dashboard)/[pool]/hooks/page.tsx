import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function Hooks() {
  return (
    <main className="px-12">
      <Card>
        <CardHeader>
          <CardTitle>My First Pool</CardTitle>
        </CardHeader>
        <CardContent>llll</CardContent>
      </Card>
    </main>
  );
}
